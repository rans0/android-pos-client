<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="suryapro.ustmobile.pos_clien">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Posclien">
        <activity android:name=".CheckoutTotalPriceActivity"></activity>
        <activity android:name=".FinishPayment" />
        <activity android:name=".OrderedActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".CheckoutIntent" />
        <activity android:name=".DaftarMenuActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>